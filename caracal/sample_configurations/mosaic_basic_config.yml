schema_version: 1.0.0

getdata:
  dataid: ['']

obsconf:
  target:
    - all
  reference_antenna: '0'

general:
  data_path: input/data
  msdir: msdir
  input: input
  output: output
  prefix: mymosaic
  init_pipeline: true

mosaic:
  enable: true
  mosaic_type: spectral
