
# attributes of the 'pipeline' instance parsed to the worker() that are required for every worker
PIPELINE_MIN_REQUIRES = [
    "input",
    "output",
    "rawdatadir",
    "msdir",
    "msname",
    "obsid",
    "CURRENT_WORKER"
]
